function diceCount(option)
{
    option.setName('dice_count');
    option.setDescription('The minimum number of dice to use the flavor text (default: 0)');
    return option;
}



module.exports = diceCount;
